# EventGenie Frontend — CLAUDE.md

## Обзор

EventGenie Frontend — это современное React/TypeScript приложение для управления событиями с интегрированными AI‑функциями. Приложение взаимодействует с backend через REST API и предоставляет интуитивный интерфейс для создания событий, генерации планов и расчёта бюджетов.

---

## 📋 Технологический стек

- **Framework:** React 18.3.1 с TypeScript 5.3.3
- **Build Tool:** Vite 6.3.5
- **Styling:** TailwindCSS 3.4.0 + shadcn/ui компоненты
- **UI Components:** Radix UI (полный набор компонентов)
- **Charts:** Recharts 2.15.2
- **Icons:** Lucide React 0.487.0
- **State Management:** React Hooks (useState, useEffect)
- **HTTP Client:** Fetch API (нативный)

---

## 📁 Структура проекта

```
frontend/
├── src/
│   ├── components/
│   │   ├── Dashboard.tsx          # Главная страница с метриками
│   │   ├── EventPlanning.tsx      # Создание событий и генерация планов
│   │   ├── Finance.tsx            # Управление бюджетами и сметами
│   │   └── ui/                    # shadcn/ui компоненты (30+ компонентов)
│   ├── api/
│   │   └── client.ts              # API клиент для взаимодействия с backend
│   ├── lib/
│   │   └── utils.ts               # Утилиты (cn, классы)
│   ├── App.tsx                    # Главный компонент с навигацией
│   └── main.tsx                   # Точка входа
├── package.json
├── vite.config.ts
└── tailwind.config.js
```

---

## 🎯 Основные компоненты

### App.tsx

- Управляет навигацией между вкладками: **Dashboard**, **Planning**, **Finance**
- Использует sidebar для переключения между разделами
- Реализует lazy loading (рендеринг только активной вкладки)

### Dashboard.tsx

- Показывает статистику событий:
  - Общее количество событий
  - Предстоящие события
  - Ожидаемое число гостей
  - Общий бюджет
- Визуализирует график распределения событий по месяцам (LineChart)
- Отображает список последних событий
- Загружает данные через `apiClient.getAllEvents()`

### EventPlanning.tsx

Форма создания события с полной валидацией:

- **Основная информация:** название, описание, тип события
- **Дата/время:** дата начала и окончания события
- **Место и люди:** локация, количество ожидаемых гостей
- **Финансы:** бюджет мероприятия
- **Формат:** offline, online или hybrid
- **Целевая аудитория:** описание аудитории
- **Контакты клиента:** компания, контакт, email, телефон

Функционал:

- Создание события через API
- Генерация плана события вызовом `apiClient.generateEventPlan(eventId)`
- Отображение сгенерированного плана (phases и tasks)
- Детальная обработка ошибок с человекочитаемыми сообщениями

### Finance.tsx

- Выбор события из списка
- Расчёт бюджета через `apiClient.calculateBudget(eventId)`
- Показ детализации по категориям расходов
- Визуализация:
  - **BarChart** — распределение бюджета по категориям
  - **PieChart** — структура расходов в процентах
- Безопасная обработка null/undefined значений при форматировании

---

## 🔌 API Клиент (`src/api/client.ts`)

**Базовый URL:** `VITE_API_URL` (по умолчанию `http://localhost:8080`)

### Интерфейсы TypeScript

```typescript
// Запрос на создание события
EventCreateRequest {
  name: string
  description?: string
  eventType: string
  eventDate: string
  eventEndDate?: string
  location: string
  expectedGuests: number
  budgetLimit: number
  targetAudience?: string
  format: 'offline' | 'online' | 'hybrid'
  clientCompany?: string
  clientContact?: string
  clientEmail?: string
  clientPhone?: string
}

// Ответ о событии
EventResponse {
  id: number
  name: string
  eventType: string
  eventDate: string
  location: string
  expectedGuests: number
  budgetLimit: number
  createdAt: string
  updatedAt: string
  // ... остальные поля
}

// План события
EventPlanResponse {
  timeline: { timeline_phases: Array<any> }
  tasks: { tasks: Array<any> }
}

// Бюджет события
BudgetResponse {
  id: number
  eventId: number
  totalAmount: number
  items: BudgetItemDto[]
  createdAt: string
  updatedAt: string
}
```

### Методы API

```typescript
// Events
createEvent(data: EventCreateRequest): Promise<EventResponse>
getEvent(id: number): Promise<EventResponse>
getAllEvents(): Promise<EventResponse[]>

// Event Plans
generateEventPlan(eventId: number): Promise<EventPlanResponse>
getEventPlan(eventId: number): Promise<EventPlanResponse>

// Budgets
calculateBudget(eventId: number): Promise<BudgetResponse>
getBudgetByEventId(eventId: number): Promise<BudgetResponse>
```

### Особенности

- Все запросы в формате JSON
- Централизованная обработка ошибок
- Логирование ошибок в консоль браузера

---

## 🎨 UI Компоненты (shadcn/ui)

Полный набор компонентов из shadcn/ui, построенных на Radix UI:

- **Формы:** Input, Textarea, Select, Checkbox, Radio, Switch
- **Навигация:** Button, Tabs, Navigation Menu, Breadcrumb
- **Отображение:** Card, Badge, Avatar, Separator, Skeleton
- **Диалоги:** Dialog, Alert Dialog, Popover, Tooltip, Hover Card
- **Данные:** Table, Progress, Slider и более 30+ компонентов

Все компоненты находятся в `src/components/ui/` и стилизуются TailwindCSS.

---

## 🔧 Особенности реализации

### Обработка дат

- Формат ISO 8601: `yyyy-MM-dd'T'HH:mm:ss`
- Форматирование через `toLocaleDateString('ru-RU')`
- Защита от `Invalid Date` через try-catch блоки

### Обработка чисел

- Форматирование денежных сумм через `toLocaleString('ru-RU')`
- Проверка на `null/undefined` перед форматированием
- Безопасное приведение `Number(value || 0)`

### Состояние загрузки

- Локальные `loading` флаги на каждом компоненте
- Показ спиннера (Loader2) во время запросов
- Дружественные пользователю сообщения об ошибках

### Валидация форм

- Проверка обязательных полей
- Валидация формата email
- Проверка положительных чисел для гостей и бюджета

---

## 🚀 Запуск и разработка

### Установка и запуск

```bash
cd frontend
npm install
npm run dev        # Dev сервер на http://localhost:5173
npm run build      # Production build
npm run preview    # Локальный просмотр сборки
```

### Переменные окружения

Создайте файл `.env.local`:

```bash
VITE_API_URL=http://localhost:8080
```

---

## 📋 Контрольный список для разработки

- ✅ Все даты используют формат ISO 8601
- ✅ Все суммы форматируются с учётом локали
- ✅ Ошибки отображаются пользователю понятными сообщениями
- ✅ Валидация проводится на клиенте перед отправкой
- ✅ CORS настроен для всех origins в режиме разработки

---

## 🔗 Связанные репозитории

- **Backend:** Jack1337322/eventgenie-backend
- **Agents Service:** Jack1337322/eventgenie-agents

---

**Последнее обновление:** Декабрь 2025
