# EventGenie Frontend

React TypeScript frontend для платформы управления мероприятиями EventGenie.

## 🚀 Технологии

- **React 18+**
- **TypeScript**
- **Vite**
- **Tailwind CSS**
- **Radix UI**
- **Recharts**

## 📋 Требования

- Node.js 20+
- npm или yarn

## ⚙️ Конфигурация

### Переменные окружения

Создайте файл `.env`:

```bash
VITE_API_URL=http://localhost:8080
```

## 🏃 Запуск

### Локальный запуск

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev
```

Приложение будет доступно по адресу http://localhost:5173

### Сборка для продакшена

```bash
npm run build
```

### Превью продакшен сборки

```bash
npm run preview
```

### Docker

```bash
docker build -t eventgenie-frontend .
docker run -p 3000:80 eventgenie-frontend
```

## 📱 Компоненты

### Основные страницы

- **Dashboard** - Обзор событий и метрики
- **EventPlanning** - Создание событий и генерация планов с AI
- **Finance** - Расчет бюджетов с помощью AI

### UI компоненты

Используется библиотека компонентов Radix UI с кастомизацией через Tailwind CSS.

## 🔗 API интеграция

Frontend взаимодействует с backend через REST API:

- `GET /api/v1/events` - Список событий
- `POST /api/v1/events` - Создание события
- `POST /api/v1/event-plans/generate/{eventId}` - Генерация плана
- `POST /api/v1/budgets/calculate/{eventId}` - Расчет бюджета

Клиент API находится в `src/api/client.ts`

## 📁 Структура проекта

```
src/
├── components/
│   ├── ui/              # UI компоненты (Radix UI)
│   ├── Dashboard.tsx    # Дашборд
│   ├── EventPlanning.tsx # Планирование событий
│   └── Finance.tsx      # Финансы и бюджеты
├── api/
│   └── client.ts        # API клиент
├── lib/
│   └── utils.ts         # Утилиты
├── App.tsx              # Главный компонент
└── main.tsx             # Entry point
```

## 🔗 Связанные репозитории

- [EventGenie Backend](https://github.com/Jack1337322/eventgenie-backend)
- [EventGenie Agents](https://github.com/Jack1337322/eventgenie-agents)

## 🎨 Стилизация

- **Tailwind CSS** для утилитарных стилей
- **CSS переменные** для тем
- **Radix UI** для доступных компонентов

## 📄 Лицензия

MIT

## 👥 Команда

Разработано с использованием GigaChat 2.0
